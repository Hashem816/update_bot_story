# شرح أوضاع التشغيل في المتجر

## 📋 نظرة عامة

يدعم المتجر ثلاثة أوضاع تشغيل رئيسية بالإضافة إلى وضع الطوارئ:

---

## 🔧 1. الوضع اليدوي (MANUAL)

**الوصف:** الوضع الافتراضي حيث يتم معالجة جميع الطلبات يدوياً من قبل الأدمن.

**الخصائص:**
- ✅ يمكن للمستخدمين تصفح المنتجات
- ✅ يمكن إنشاء طلبات جديدة
- ✅ يتم تنفيذ الطلبات يدوياً من قبل الأدمن
- ✅ جميع العمليات المالية نشطة (شحن رصيد، شراء)

**متى يُستخدم:**
- عند بداية تشغيل المتجر
- عندما لا يكون هناك نظام تلقائي متاح
- للحفاظ على جودة الخدمة والتحكم الكامل

---

## 🤖 2. الوضع التلقائي (AUTO)

**الوصف:** يتم تنفيذ الطلبات تلقائياً عبر API الموردين.

**الخصائص:**
- ✅ يمكن للمستخدمين تصفح المنتجات
- ✅ يمكن إنشاء طلبات جديدة
- 🤖 يتم تنفيذ الطلبات تلقائياً عبر API
- ✅ جميع العمليات المالية نشطة

**متى يُستخدم:**
- عند توفر API موثوق من الموردين
- لتسريع تنفيذ الطلبات
- عند وجود حجم كبير من الطلبات

---

## 🛠 3. وضع الصيانة (MAINTENANCE)

**الوصف:** وضع مؤقت لإجراء صيانة أو تحديثات على المتجر.

**الخصائص:**
- ✅ يمكن للمستخدمين تصفح المنتجات
- ❌ **لا يمكن إنشاء طلبات جديدة**
- ✅ يمكن شحن الرصيد
- ✅ الطلبات الحالية تستمر في المعالجة
- ✅ الأدمن يمكنه العمل بشكل طبيعي

**متى يُستخدم:**
- عند تحديث قاعدة البيانات
- عند إضافة منتجات جديدة
- عند تعديل الأسعار
- عند إجراء صيانة دورية

**رسالة للمستخدمين:**
> 🛠 المتجر في حالة صيانة حالياً، سنعود قريباً.

---

## 🚨 4. وضع الطوارئ (EMERGENCY_STOP)

**الوصف:** وضع حرج يوقف **جميع** العمليات المالية والإدارية فوراً.

**الخصائص:**
- ❌ **لا يمكن إنشاء طلبات جديدة**
- ❌ **لا يمكن شحن الرصيد**
- ❌ **لا يمكن معالجة الطلبات الحالية**
- ❌ **جميع العمليات المالية متوقفة**
- ✅ يمكن للمستخدمين فقط عرض حساباتهم
- ⚠️ الأدمن فقط يمكنه إلغاء هذا الوضع

**متى يُستخدم:**
- عند اكتشاف ثغرة أمنية
- عند حدوث خطأ مالي كبير
- عند اختراق محتمل
- في حالات الطوارئ القصوى

**رسالة للمستخدمين:**
> ⚠️ المتجر في وضع الطوارئ. جميع العمليات متوقفة مؤقتاً.

---

## 🔄 الفروقات الجوهرية

| الميزة | يدوي | تلقائي | صيانة | طوارئ |
|-------|------|--------|-------|-------|
| تصفح المنتجات | ✅ | ✅ | ✅ | ✅ |
| إنشاء طلبات | ✅ | ✅ | ❌ | ❌ |
| شحن رصيد | ✅ | ✅ | ✅ | ❌ |
| معالجة طلبات حالية | ✅ | ✅ | ✅ | ❌ |
| عمليات مالية | ✅ | ✅ | ✅ | ❌ |
| عمليات إدارية | ✅ | ✅ | ✅ | ⚠️ محدودة |

---

## 💡 ملاحظات مهمة

### وضع الصيانة:
- **الهدف:** صيانة مجدولة ومخططة
- **التأثير:** محدود (فقط الطلبات الجديدة)
- **المدة:** قصيرة (دقائق إلى ساعات)
- **الشفافية:** يتم إخبار المستخدمين مسبقاً

### وضع الطوارئ:
- **الهدف:** حماية فورية من خطر محتمل
- **التأثير:** شامل (جميع العمليات)
- **المدة:** حتى حل المشكلة
- **الشفافية:** يتم التفعيل فوراً دون إنذار

---

## 🔐 التحكم بالأوضاع

### تغيير الوضع العادي (يدوي/تلقائي/صيانة):
```python
await db_manager.set_setting('store_mode', 'MANUAL')  # أو AUTO أو MAINTENANCE
```

### تفعيل وضع الطوارئ:
```python
await db_manager.set_setting('emergency_stop', '1')  # تفعيل
await db_manager.set_setting('emergency_stop', '0')  # إلغاء
```

### التحقق من الوضع الحالي:
```python
store_mode = await db_manager.get_setting('store_mode', 'MANUAL')
emergency_stop = await db_manager.get_setting('emergency_stop', '0')
```

---

## 📝 أمثلة على الاستخدام

### مثال 1: صيانة مجدولة
```
1. الإعلان للمستخدمين عن الصيانة
2. تفعيل وضع الصيانة
3. إجراء التحديثات
4. العودة للوضع اليدوي/التلقائي
```

### مثال 2: طوارئ أمنية
```
1. اكتشاف ثغرة أمنية
2. تفعيل وضع الطوارئ فوراً
3. إيقاف جميع العمليات
4. معالجة المشكلة
5. إلغاء وضع الطوارئ
6. العودة للوضع الطبيعي
```

---

## 🎯 التوصيات

1. **استخدم وضع الصيانة** للتحديثات المخططة
2. **استخدم وضع الطوارئ** فقط في الحالات الحرجة
3. **أخبر المستخدمين** قبل تفعيل وضع الصيانة
4. **سجل جميع التغييرات** في Audit Logs
5. **اختبر الأوضاع** في بيئة التطوير أولاً

---

تم التوثيق بواسطة: Production Refactor v2.3
التاريخ: 2024
