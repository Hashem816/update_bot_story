# 🤖 نظام متجر تيليجرام الاحترافي (v2.2 Ultimate)

هذا الإصدار هو التطوير الشامل المبني على متطلبات التشغيل اليدوي الذكي، مع نظام رتب إدارية متقدم وتحكم كامل في الأسعار والعملات.

## 🌟 المميزات الجديدة (v2.2)
- **نظام الرتب الثلاثي**:
  - `Super Admin`: تحكم كامل (أوضاع، أسعار، مستخدمين، رتب).
  - `Operator`: إدارة الطلبات، المنتجات، وتأكيد الدفع.
  - `Support`: مراقبة الطلبات والتواصل مع المستخدمين فقط.
- **نظام التسعير المرن (Dynamic Pricing)**:
  - إمكانية تعديل سعر الدولار داخل البوت بضغطة زر.
  - حساب تلقائي لأسعار المنتجات بالليرة السورية بناءً على السعر المحدد.
- **دورة الدفع اليدوي الموثوقة**:
  - يختار المستخدم المنتج -> يختار طريقة الدفع -> يرسل صورة الإيصال.
  - تصل الصورة فوراً للأدمن مع أزرار (قبول/رفض) لتفعيل الطلب.
- **إدارة المنتجات المتكاملة**:
  - إضافة أقسام ومنتجات جديدة بالكامل من داخل تلجرام عبر حوار تفاعلي (Wizard).
- **الأمان والخصوصية**:
  - نظام حماية من السبام (Throttling).
  - سجلات مالية وسجل ثقة لكل العمليات الحساسة.

## 📁 هيكل المشروع
- `/handlers/admin.py`: إدارة المستخدمين والرتب.
- `/handlers/products.py`: (جديد) إضافة وإدارة المنتجات والأقسام.
- `/handlers/admin_modes.py`: التحكم في أوضاع المتجر وسعر الدولار.
- `/handlers/admin_orders.py`: معالجة الطلبات ومراجعة الإيصالات.
- `/middlewares/auth.py`: نظام الصلاحيات المبني على الرتب.

## 🚀 التشغيل
1. قم بتثبيت المتطلبات: `pip install aiogram aiosqlite python-dotenv aiohttp`
2. قم بتحديث ملف `.env` بوضع `BOT_TOKEN` و `ADMIN_ID` (معرفك الشخصي ليكون Super Admin).
3. شغل البوت: `python main.py`

## 🛠 تعليمات الإدارة
- عند تشغيل البوت لأول مرة، سيتم تعيين صاحب الـ `ADMIN_ID` كرتبة `SUPER_ADMIN`.
- يمكنك إضافة "طرق الدفع" من لوحة التحكم (مثل سيريتيل كاش) ووضع تعليمات التحويل الخاصة بك.
- لتغيير سعر الدولار، اذهب إلى `لوحة التحكم` -> `سعر الدولار`.

---
**تم التطوير لضمان أعلى مستويات الأمان والسهولة في الإدارة من الهاتف.**
